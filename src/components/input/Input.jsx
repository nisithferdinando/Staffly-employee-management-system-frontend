import React from 'react'

const Input = ({
    label,
    type="text",
    name,
    value="",
    onChange=()=>{},
    placeholder="",
    disabled=false,
    size,
    required=false,

}) => {
  return (
    <div>Input</div>
  )
}

export default Input